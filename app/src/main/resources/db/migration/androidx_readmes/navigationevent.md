# navigationevent

The Navigation Event library provides a KMP-first API for handling system back as well as Predictive Back. 

[[API Reference](https://developer.android.com/reference/kotlin/androidx/navigationevent/package-summary)]

## Declaring dependencies

To add a dependency on navigationevent, you must add the Google Maven repository to your project. Read [Google's Maven repository](https://developer.android.com/studio/build/dependencies#google-maven) for more information.

Add the dependencies for the artifacts you need in the `build.gradle` file for your app or module:

### Groovy
```
dependencies {
    implementation "androidx.navigationevent:navigationevent:1.0.1"
}
```

### Kotlin
```
dependencies {
    implementation("androidx.navigationevent:navigationevent:1.0.1")
}
```

For more information about dependencies, see Add build dependencies.

## Issue tracker
[Issue Tracker](https://issuetracker.google.com/issues?q=componentid:1777955)
